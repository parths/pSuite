# pSuite

Practice apps



* Use [src_ant_build](./src_ant_build) for older ant build. Might not contain all modules completed. Current completed modules are FileBrowser and VideoPlayer.
* Use [src_gradle](./src_gradle) for the upgraded gradle project.
